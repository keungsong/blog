let fs,path;_673‍.x([["filRemover",()=>filRemover]]);_673‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_673‍.w("path",[["default",["path"],function(v){path=v}]]);



const filRemover = (filename) =>{
    fs.unlink(path.join(__dirname, '../uploads', filename), function (err){
        if(err && err.code == "ENOENT"){
            // file doesn't exist
            console.log(`file ${filename} doesn't exist, won't remove it.`)
        }else if(err){
            console.log(`Error occured while trying to remove file ${filename}`)
        }else{
            console.log(`removed ${filename} `);
        }
    });
}

